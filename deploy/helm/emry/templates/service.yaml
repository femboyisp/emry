apiVersion: v1
kind: Service
metadata:
  name: {{ include "emry.fullname" . }}
  labels:
    {{- include "emry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "emry.selectorLabels" . | nindent 4 }}
